import { StyleSheet } from "react-native"

const styles = StyleSheet.create({
    mr20: {
        marginRight: 20
    },

    root: {
        alignItems: 'center',
        marginTop: '30%',
        flex: 1,
    },
    language: {
        flexDirection: 'row',
        position: 'absolute',
        bottom: '10%',
    },
    icon: {
        width: 50,
    },
    title: {
        position: 'absolute',
        bottom: '2%',
    }

})

export default styles