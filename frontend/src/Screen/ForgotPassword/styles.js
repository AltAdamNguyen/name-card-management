import { StyleSheet } from "react-native"

const styles = StyleSheet.create({
    root: {
        alignItems: 'center',
        marginTop: '30%'
    },
    header: {
        
    }
})

export default styles